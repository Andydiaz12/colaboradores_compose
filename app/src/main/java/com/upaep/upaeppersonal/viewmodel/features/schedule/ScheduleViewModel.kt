package com.upaep.upaeppersonal.viewmodel.features.schedule

import androidx.lifecycle.ViewModel
import com.upaep.upaeppersonal.model.repository.ScheduleRepository
import javax.inject.Inject

class ScheduleViewModel @Inject constructor(scheduleRepository: ScheduleRepository) : ViewModel() {
    init {

    }
}